services:
  - type: web
    name: brokernote-backend
    env: python
    pythonVersion: "3.11.0"
    buildCommand: pip install --prefer-binary -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
